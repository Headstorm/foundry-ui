{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "test-react": "cd packages/hs-react-ui && npm install && npm test && npm run lint-ci",
    "build-react": "cd packages/hs-react-ui && npm install && npm run build && npm run lint-ci && npm run build-storybook"
  }
}