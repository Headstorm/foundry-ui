{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "test-react": "cd packages/hs-react-ui && npm install && npm test",
    "build-react": "cd packages/hs-react-ui && npm install && npm run build && npm run build-storybook",
    "test-accessibility": "cd packages/hs-react-ui && npm install && npm run start && npm test-pa11y"
  }
}
